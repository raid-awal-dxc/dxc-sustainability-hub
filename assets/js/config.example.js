// Template only.
// This file stays loaded by the pages, but the runtime defaults in config.js
// now handle both local development and the deployed site.
// If you need local overrides in the future, copy this file to a separate
// untracked config file and load that instead of editing the runtime defaults.
